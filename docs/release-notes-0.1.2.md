[中文](#cn-v0.1.2) | [English](#en-v0.1.2)

0.1.2 是一次「节点内富文本 + 结构整理 + 图片增强 + 回写保真 + **逐条对齐 Obsidian 官方交互口径**」的版本：新增**节点内富文本（方案 B）**——节点里的链接渲染为**可点文本**（多链接各自可点、URL 恢复为可点地址、悬停预览跟指针）、**轻标记**（粗体 / 斜体 / 行内代码 / 删除线）、**超长节点截断展示**与**多 token 编辑后原位保真**；新增**混排双链拆分**（选中节点命令、编辑后自动、全文批量三种入口），段落（文本）节点支持**首行图片渲染**；图片链路做了成体系的修复与性能优化（加载期尺寸不回写、调宽写入合并、缓存 LRU / 跨会话）；回写层集中修复了一批**编辑后数据降级**问题（一行多图的嵌入语法、一行多链接 / 多 URL 的非首个 token、链接引用定义行），并再次调整「纯双链化」写入语义。同时修复了**打开导图时的视口偏差**与**「先亮根节点居中、再跳成整体居中」的可见跳变**（首帧几何三坑，见「修复」），设置面板调整改为**原地生效、不再重建引擎**（性能阈值 / 主题不再重置视图，见「性能」）。底层 Markdown 无损往返、三类链接图标、官方尺寸语法、PNG 导出、搜索、性能模式均保持兼容。本版另含一轮**对照官方指南与 API 的合规审计**（修复发布阻断、许可声明分发、视图与引擎的生命周期泄漏，收回 ESLint 配置层的三处静默放宽，并补上 CSS 检查与发布元数据门禁——见「工程」）。

<h3 id="cn-v0.1.2">新增</h3>

* **混排双链拆分**：与描述文字混排的文档 / 附件双链（如 `关于 [[冬天]] 和 [[秋天]] 的相关问题`）可抽离为子节点——父节点保留描述文字（链接处替换为可见名），每条链接一个子节点（各自拥有图标 / 悬停 / 点击）。三种入口：① 命令「拆分节点内双链为子节点」；② 编辑此类节点后**自动拆分**（设置项「自动拆分混排双链」，**默认开启**，可关闭；检查对象为编辑期捕获的节点，编辑提交后即使快速切换选中节点也不会漏拆）；③ 命令「拆分文档内全部混排双链」批量处理整篇（含未编辑的存量节点）。图片与外链不拆分、链接引用定义行不拆分、同一目标去重、重复执行幂等
* **段落节点首行图片**：段落（文本）节点首行的 `![[图.png]]` 现在会渲染为节点图片（可更换 / 移除）；段落中的链接语法仍按原文逐字保真
* **节点内富文本（方案 B）**：节点里的链接渲染为**可点文本**（点击跳转、`Ctrl`/`Command`+点击在新标签打开）——**一行多枚链接各自可点**（此前只有第一枚生效）、悬停预览**两级制**（悬停某枚链接文字 → 预览那一枚；悬停节点其它位置 → 预览该节点承载的链接）；**URL 与其它内容混排时恢复为可点地址**（此前一律仅图标）；轻标记 `**粗体**` / `*斜体*` / `` `行内代码` `` / `~~删除线~~` 在节点内直接呈现（**无链接的节点同样生效**），文件原文逐字保真；**段落（多行）节点里的链接同样可点**。含链接 / 轻标记 / 超长文本的节点改由**插件文本弹窗**编辑（双击 / 右键「编辑文本」/ F2 三入口共用，弹窗里是**全文**），自绘内容样式随导出 SVG 内联带出（PNG 导出保真）
* **超长节点截断展示**：超过 2000 字的节点只渲染开头 + `…`（悬停有说明），完整内容仍在文件中、双击可查看 / 编辑全文
* **节点编辑弹窗：原文模式**（编辑入口按节点类型分流）：**纯链接节点**仍是「改别名」（原语义不变）；**其余富节点**（混排链接 / URL / 轻标记 / 超长）改为编辑**文件里的那一行**——`[[双链]]`、URL、`**粗体**` 语法全部可见可改，弹窗内实时显示「**节点将显示为**」预览，保存后**逐字写回**该行（重解析 → 行内字段整体重建 ⇒ 未编辑态）。此前这类节点编辑的是**显示文本**：双链只剩剥壳名、外链（icon-only）连影子都没有（用户实测反馈）
* **多 token 编辑后「原位」保真**：一行内多枚链接 / URL / 图在编辑节点后按**原文位置**写回（此前一律挪到行尾）；无法定位时才回落到行尾追加
* **交互对齐 Obsidian 官方口径**（逐条对照官方帮助库）：① **打开链接的落点补齐两档**——`Ctrl`/`Command`+`Alt`+点击＝**新标签组**，再加 `Shift`＝**新窗口**（此前只有「新标签」）；② **未解析链接弱化**——指向尚不存在目标的链接显示为**弱化配色**（同阅读视图），且悬停不再弹预览；③ **行内语法扩至官方清单**——新增 `==高亮==`、`__粗体__` / `_斜体_`、`***粗斜***`、双反引号代码跨度，并正确处理反斜杠转义（`\*a\*` 显示 `*a*`，此前会被误渲染成斜体）与 `%%注释%%`（节点内隐藏，文件里保留），与阅读视图一致；④ **拖入系统文件支持任意类型**（此前有类型白名单），可嵌入的附件默认写嵌入语法 `![[报告.pdf]]`，按住 `Ctrl`（Win/Linux）/`Option`（mac）拖入则**不复制**、改插入指向原位置的绝对链接 `[文件名](<file:///…>)`（点击交系统默认应用）——与 Obsidian 的拖放一致

<h3>修复</h3>

* **回写保真：一行多张库内图片**：`![[a.png]] 与 ![[b.png]]` 此前**任何保存**都会把第二张图的嵌入语法降级为纯文件名文本（被误判为「残留链接」）；现按目标类型区分，图片类嵌入完整保留
* **回写保真：一行多链接 / 多 URL / 多图**：编辑此类节点后，非首个 token 此前会降级为文本（多裸 URL 连内容都会整体丢失）；现按原文完整保留——内容与语法不丢，为保持行结构稳定，额外 token 统一附于行尾，**所在位置可能与原文不同**（本版后续已改为**原位写回**，见「新增」最后一条）
* **回写保真：链接引用定义行**（参考式 `[ref]: …` / 脚注 `[^1]: …`）：此前编辑含裸 URL 的定义行会把 URL 挪到行尾、`[ref]` 引用全部失效；现定义行整体按纯文本保真，拆分命令同样跳过定义行
* **frontmatter 写盘取磁盘现值**（数据安全）：写盘前重读文件，文件头以**磁盘当前内容**为准——视图打开期间在属性面板 / 其它窗格 / 其它设备（同步）改动的属性，不再被加载时的旧快照覆盖（此前偶有「笔记属性丢失」）
* **frontmatter 解析边界对齐 Obsidian**：空属性块（`---` 与 `---` 相邻、属性被删光）不再被并进正文；YAML 块标量（`|` / `>`）内部的 `---` 不再截断属性；开头 BOM 计入文件头原样保留（此前会导致首个标题降级为段落、首个列表项多出假缩进）
* **图片：加载期自动尺寸不再回写**：按原始比例自动算出的显示尺寸（非用户意图）不再写出 `|宽度`——此前从未编辑过的行可能在保存后凭空多出尺寸参数
* **图片：右键「移除图片」后不再复活**：此前在列表 / 标题节点移除图片，下一次保存会把原图写回（图片回写元数据被清理后，「图片已移除」判定失效、整行被逐字写回）；现判定不再依赖字段残留——`image` 为空且行内仍有图片语法（行内 `![[…]]` / `![…](…)`）即按移除处理，与段落节点同口径
* **拆分：运行期资源地址不再污染笔记**：此前图片节点的混排拆分会把运行期资源地址（`app://…#图.png?时间戳`）当作附件抽成子节点、URL 编码片段被写进父文本；现与序列化同入口携带 `app`，行内出现 `app://` 一律放弃拆分
* **删除笔记导致链接被静默降级为纯文本**（数据安全，用户实测反馈）：此前删除笔记（或移入 `.trash`）时，插件会把导图节点上的链接引用清空——但节点文本与行内台账不动，于是下一次保存把 `[[笔记A]]` 写成了纯文本 `笔记A`（同一行里其它链接因台账仍在而保留语法，表现为「首个链接降级」），**不可逆**。现改为：**链接只改不删**——删除/回收站场景保留为「未解析链接」（与 Obsidian 一致，同名笔记重建即恢复），重命名仍照旧改写；**附件整条删除**——引用与节点内的可见文字一起去掉（只清引用会留下「报告.pdf」这样的纯文本残留），因此变成空的**叶子节点连节点一并摘除**，带子节点的节点则退化为保留引用（不连带删子树）；**内嵌图片**仍按删除即清除（目标缺失会在节点上留下坏死图块）
* **超长节点导致白屏 / 卡死后关闭**：根因在引擎默认 SVG 文本路径——其换行逐字符迭代、每加一字重新拼接整行并**重新测量一次**，单行开销随字数**二次增长**（无头 Chrome 同机实测：20k 字单行 ≈ **+2.8 秒 / 节点**，整轮 1.1s → 3.9s），而原「超长文本回落引擎」策略恰好把最长的行送进该通道。现改为**长文本一律自绘接管 + 截断展示**：同负载整轮回到 **1.1 秒**（与无长文本基线持平）
* **轻标记在无链接节点不生效**：`**粗体**` 此前只有在**含链接**的节点里才渲染，否则原样显示标记符；现「含链接**或**轻标记」都接管
* **导出 PNG 掉样式**：自绘内容的布局 / 排版 / 配色 / 轻标记样式改为**全部内联在元素上**（引擎导出只注入自身 CSS，不加载插件 styles.css；此前依赖类规则会让导出图尺寸与排版错乱）
* **富节点双击无响应**：引擎的节点编辑框对自绘节点静默失效；现双击 / 右键「编辑文本」/ F2 统一走插件文本弹窗（弹窗内为全文）
* **打开导图首次渲染就偏移**（用户实测反馈）：打开 `.mindmap.md` / 文档模式导图时内容整体偏向一侧、点「适应画布」能立刻拉回（故一直像「偶发」）。根因是**首帧居中用陈旧几何**——引擎只在创建与 `resize()` 时缓存容器矩形（`elRect` / `width` / `height`），而首帧后工作区布局 settle、文档模式工具栏重建都会改容器尺寸与位置且**不触发 `view.onResize`**，缩放锚点与内容包围盒换算便同时按旧画布算。现取视口前先按**实时容器**同步引擎几何再换算（`getLiveCanvasRect` / `syncCanvasGeometry`），内容包围盒也改以实时容器为原点
* **保存视口可能把内容推出画布**：保存的视口按「当时的容器与内容布局」记录，容器尺寸大改或内容变化后恢复它可能让画面停在画布外（打开即空白）。现恢复后做**落界校验**：明确判定不可见则放弃恢复、回退默认居中；无法判定（性能模式、引擎中间态）时保持原状，不误伤用户自己的视口
* **打开时先亮「根节点居中」再跳成「整体居中」**（可见跳变）：引擎默认 `initRootNodePosition = [center, center]`，首次布局把**根节点**摆在画布中心，而插件的视口设置发生在 150ms 兜底定时器里 ⇒ 中间态被绘制、随后跳变。现在**在首次布局落地的事件回调里同帧应用视口**（`node_tree_render_end`，属布局任务内提交，浏览器不会绘制中间态），并为「布局后内容还会变」的窗口留 2 次重算预算（`resize` 触发的再布局、图片尺寸回灌都会改包围盒），用尽即停手，不抢用户之后的视口
* **含图笔记的居中在图片尺寸落定后才准**：图片尺寸探测改为不挡首帧后，真实尺寸回灌会改节点尺寸与包围盒；现在回灌后（**仅当当前视口仍是打开时自动设置的那个**）补一次居中——用户已平移 / 缩放则完全不打扰

<h3>性能</h3>

* **图片调宽写入合并**：拖拽按最小步长（8px）合并写入、松手补写最终尺寸——整树重排开销降一个量级；陈旧帧按会话所属引擎校验，不写进新引擎
* **图片尺寸缓存真 LRU**：命中提升，热点图不再被冷图挤出后重复解码
* **跨会话尺寸缓存**：经官方 `App#saveLocalStorage` 按库隔离——第二次打开同一库的图片尺寸零解码探测（外链与探测失败不落盘、数据损坏静默降级）
* **图片弹窗**：预览按解析结果去重，输入中间态不再逐键重建 DOM
* **节点渲染成本封顶**：自绘路径的每节点上界 = 2000 字（与文本总长无关，超长节点不再随字数变慢）；行内段序列按**原文内容寻址**缓存，拖拽 / 缩放期间同一行不再重复解析
* **设置面板调整「即时生效、不重建引擎」**：此前应用设置对每个打开的视图一律重建（整树深拷贝 + 销毁重建引擎 + 全量重渲染 + 工具栏重建），而性能阈值是 step 100 的滑块、拖一次即几十轮。现按**按键差集**分流——**主题**（`setThemeConfig`）与**性能模式 / 阈值**（`updateConfig`，引擎显式支持运行时开关）**原地生效**；**默认布局 / 默认连线样式**对已打开的图本就不生效，直接跳过；只有**拖拽开关**与**语言**仍需重建（引擎侧只有创建期通道：拖拽由创建期注册的 Drag 插件承担，语言在创建期被闭包捕获）
* **渲染经济：一次动作至多一次布局落地**：审计插件侧全部 `render()` 调用点，查出并修掉一处冗余（关闭性能模式时与引擎自带的一次重复 ⇒ 白白多一轮全树渲染）；`verify:visual` 新增 render-eco 探针，把 8 条代表性动作的布局落地次数钉成回归闸门
* **大图撤销深度的内存预算**：命令历史逐条保存「整树 JSON 快照」，单条 ≈ 节点数 × 0.4KB，原先的固定条数上限让**单视图历史常驻内存随图规模失控**（旧策略 500 节点图 ≈ 91MB/视图，反重于 2000 节点图 ≈ 70MB）。现按 **30MB/视图的内存预算反推条数上限**：500 节点 → **163 条**、2000 节点 → **40 条**、超大图保底 30 条（预算宽裕的小图仍是引擎默认 500 条）——单视图历史内存钉在预算内，撤销深度仍远超日常所需

<h3>行为变化提示</h3>

* **链接写入语义再修订**（0.1.1「无条件覆盖」→ 本版**仅「完全空白」节点覆盖**）：把链接（文档 / 附件 / URL）挂到节点时——**已有任何内容的节点**（文字 / 原链接 / 图片 / 行内额外 token）一律**建为子节点**，节点原内容完全不动（行内多链接不再被覆盖牵动）；只有**完全空白**节点才覆盖为链接节点。URL **单独成节点**时仍是「仅图标」（节点文本为空；与其它内容混排时显示为可点地址）。注意：「一键改链」入口随之取消——改链请先「清除链接」再添加，或直接编辑节点文本
* **节点内链接现在是可点文本**（此前只有第一枚可点、URL 仅图标）；含链接 / 轻标记 / 超长文本的节点**不再有引擎原位编辑框**，双击弹出插件弹窗
* **编辑弹窗分两种模式**（同上「新增」条目）：纯链接节点 = 改别名（不变）；其余富节点 = 编辑**原文行**（此前编辑的是显示文本，链接语法与外链都看不见）。原文模式下取消或未改动不写盘
* **节点宽度（拖左右边框）：只有自绘节点能拖**——含链接 / 轻标记 / 超长文本（>2000 字）的节点自绘渲染，拖宽生效且高度随换行跟随；**纯文本与含图节点不显示该手柄**（引擎的 SVG 文本路径不支持按节点改换行宽度，此前手柄可拖但毫无反应）。拖宽仅当前会话有效（正文保持纯 Markdown，宽度不落文件），重开回落 500px 折行上限
* **超过 2000 字的节点只显示开头**（`…` 标示，悬停有说明）；文件内容与 `data.text` 不受影响，双击可查看 / 编辑全文
* **含图节点不受这些渲染变化影响**：图片仍由引擎图片通道渲染（图片节点的混排文本不再叠加文档页图标）；**含图 + 超长文本**的节点仍由引擎换行承担（无法接管），建议把该节点拆短
* **`F2` 对齐官方**：**未选中节点时** F2 交回 Obsidian＝**重命名当前文件**（官方语义）；选中节点时仍是编辑该节点（中心节点被选中时同理——中心节点文本即文件名）。此前无条件吞键，等于在导图视图里用不了官方 F2
* **新增设置「自动更新链接」**（对应官方「自动更新内部链接」，**默认开启**）：关闭后重命名不再改写链接（变未解析）、删除不再清理引用与附件，与官方关掉该设置的表现一致
* **链接语法跟随官方 Wikilink 设置**：官方「使用 \[\[Wikilinks\]\]」**开启**（官方默认）行为完全不变（恒写 `[[双链]]`、保留自绘文档页图标）；用户**关闭**该设置时，新文档链接写 md 形态 `[显示名](路径.md)`（走 hyperlink 通道），重命名改写同样支持该形态（此前只认 `[[…]]`，md 形态链接改名后会失效）
* **拖入类型白名单取消**：库内文件、系统文件、链接弹窗三处的「附件」判定统一为**解析侧口径**（非文档扩展名即附件，无扩展名视为文档）——手写 `[[说明.txt]]` 能显示、拖入 `.txt` 也同样能挂上（此前拖入会被拒、手写却可以，属两侧口径不一致）。随之删除三份失效白名单与两条失效文案
* **不可渲染的库内文件点击改为交系统默认应用**（此前只有音视频外跳，其余弹「无法预览」）——白名单取消后任意扩展名都可能入库，弹提示等于「链了打不开」
* **大图上的撤销步数减少**：历史上限改为按内存预算反推（见「性能」）——可回退步数随图规模收敛（500 节点 163 步 / 2000 节点 40 步 / 保底 30 步），换来单视图历史内存不超过 30MB

<h3>工程</h3>

* **测试**（全量 42 个文件 / 1327 例）：新增 `tests/view-split-links.test.ts`（自动拆分：设置开关 / 未编辑 / 编辑中与候选集语义）、`tests/agents-md-sync.test.ts`（**文档-代码同步契约**：`src/**` 与 `tests/**` 每个文件必须登记在 AGENTS.md，漏登记即红灯）；`tests/links-split.test.ts` 扩为全规则矩阵（含链接引用定义行跳过、拆分后多 token 重建）；`tests/md-roundtrip.test.ts` 补回写保真用例组（一行多图、多链接 / 多 URL / 多图编辑后保真、定义行 4 形态 + 不误伤对照）；frontmatter（CRLF / 空块 / 块标量 / BOM）、图片尺寸与缓存、自动拆分候选、引擎事件转发契约等回归补强
* **文档**：AGENTS.md 重构为 **K 编号分组体系**（K1–K49 稳定引用 ID + 「新增功能检查清单」+ 「本文件维护规则」）；标准文档同步（拆分规则、回写保真与已知降级表）；README 修正链接写入语义
* **校验**：类型检查 0 错误、eslint 0 警告、全量测试全绿
* **测试（本轮新增）**：全量 **44 文件 / 1401 例**；新增 `tests/node-inline-content.test.ts`（段序列、接管判定、轻标记、超长截断、内联样式契约）、`tests/md-line-write.test.ts`（原文写入：行内字段整体重建、逐字往返锁、多行、未闭合语法）、`tests/view-wikilink.test.ts` 锚点级悬停 6 例、`tests/view-hotkeys.test.ts` 编辑入口两种模式分流；`tests/md-inline.test.ts` 补台账登记边界；`npm run verify:visual` 扩为 **9 个场景 + 6 个探针**（自绘装配、测宽同源、合成点击命中锚点、导出内联样式、30 富节点规模、超长截断、段落可点）
* **单一台账**：额外 token 的 `mdExtraTokens` 并入 `mdSegments`（`first` 区分首 token / 额外 token），解析、拆分重建、清除链接三处各只有一处实现
* **修复（撤销失效）**：**拆分文档内全部混排双链**（以及重命名/删除文件的引用更新）此前用引擎 `setData` 落数据——它内部 `clearHistory()` 会把历史清成「只剩新状态一条」，之后 `Ctrl`/`Command`+`Z` **永远无事发生**（用户实测）。现统一走 `engine/mindmap.replaceMindMapData`（`updateData` + `CLEAR_ACTIVE_NODE`）：只追加一条历史，**一次撤销即回到操作前**；契约由 vendor-contract（引擎必须保留 `updateData`/`CLEAR_ACTIVE_NODE`）与拆分、引用更新三处回归锁定
* **测试（对齐轮）**：全量 **44 文件 / 1437 例**；`tests/node-inline-content.test.ts` 补轻标记扩展 / 转义字符集 / 注释 / 未解析标记，`tests/view-wikilink.test.ts` 补修饰键矩阵、中键（`auxclick`）与**悬停预览两级制**用例（锚点优先含「首链为外链时的行内库内 md 链接」实测场景、节点级兜底、锚点为外链时回落）；**2026-09-16 定稿**：曾一度只留锚点级（常规悬停静默无反应）或只留节点级（首链为外链时行内库内链接无法预览），且「未解析不预览」的解析预检与核心职责重复——现为锚点优先 + 节点级兜底、均不做解析预检；`tests/view-dnd.test.ts` 重写为「任意类型可导入 / `Ctrl` 绝对链接 / 无扩展名走文档通道」，`tests/url.test.ts` 补 `file:///` 绝对链接三形态往返，`tests/view-node-actions.test.ts` 补 `applyExternalLink` 与 `mdEmbed`；`npm run verify:visual` 扩为 **10 个场景 + 6 个探针**（新增 `syntax` 场景：`<mark>` / `__粗__` / 转义 / 注释 / `is-unresolved`，并含两条负向自检：解析桩恒真、注释不隐藏，均实测报红）
* **修复（拖边框改宽后节点高度不跟随）**：自绘（富）节点「选中后拖动左右边框改宽」时宽度变、高度不变。根因在引擎分工：引擎只对自绘节点给出 `ew-resize` 边框手柄，拖拽中每帧只把宽度写进节点字段 `node.customTextWidth`（`reRender([])` **不重建**自绘内容），松手也不重建（整树 `render()` / `needLayout` 均无效，无头实测元素 inline width 为空），而自绘节点的宽高**全部来自内容元素的离屏测宽**。现两处配合：自绘内容读 `customTextWidth` 落到元素宽度（`maxWidth` 同步放开，否则 500 上限会把更宽的拖拽钳回），并在 `dragModifyNodeWidthEnd` 时重建该节点自绘内容 → 新宽度下重新测宽，**高度随之变化**（实测 500 → 240 后高度增大）。已知边界：拖拽过程中高度滞后一拍（引擎每帧不重建自绘内容），松手即校正；**宽度不落文件**（正文保持纯 Markdown），重开回落 500 折行上限
* **修复（图片拖动调宽：保存好几次 + 卡顿）**：拖动图片调宽时每过一个步长就写一次引擎命令，而引擎 `Command.exec` 对非白名单命令一律 `addHistory()`（**整树深拷贝 + 序列化比对**，还被 100ms 防抖包装）并 `emit('data_change')` ⇒ 视图层的自动保存调度被反复触发（慢拖时落盘好几次）、状态栏/标题逐帧重算，全树深拷贝本身也是拖动卡顿的主因；撤销链还被切成多段（一次调宽要按很多次 `Ctrl`/`Command`+`Z`）。现**帧内只改数据 + 重绘**（不上历史、不派发 `data_change`），**松手时一次性走命令**：一次拖动 = **一条历史 + 一次保存调度**，一次撤销即回到拖动前；拖回原尺寸则不提交（不产生空历史）。回归：`tests/feature-teardown.test.ts`（帧内零命令 / 收尾恰好一次 / 步长合并 / 拖回原尺寸）+ `verify:visual` 新增 history 探针（真实引擎实测帧内 0 历史 0 保存调度、收尾各 1，并做过负向自检）
* **修复（纯文本节点上的「死手柄」）**：选中**纯文本节点**（引擎 SVG 文本）拖动左右边框毫无反应——引擎的手柄门禁只看全局开关（本插件开自绘通道 ⇒ **每个节点**都有手柄），而宽度只有**自绘节点**认：纯文本走 `textAutoWrapWidth` 的 SVG 文本路径，**完全不读** `customTextWidth`。现按节点收窄门禁（`gateNodeWidthHandles`，判据与自绘接管同源 `shouldSelfDrawNode`）：**手柄只留在拖宽真正生效的节点上**（含链接/轻标记/超长文本的自绘节点、拖完高度跟随），纯文本与含图节点不再显示这个拖不动的边框。回归：`tests/view-node-width.test.ts` + 无头 handle 探针（纯文本 0 个手柄 / 自绘 2 个）
* **修复（复制链接给裸路径）**：节点右键「复制链接」此前复制的是**通道原文**——md 形态链接（官方关闭「使用 \[\[Wikilinks\]\]」时新建的那种）只拿到裸路径 `目录/说明.md`，粘回笔记是纯文本而非链接。现改为复制**文件里的写法**（由 `md-serialize.renderHyperlink` 唯一产出：`[[笔记|别名]]` / `[说明书](目录/说明.md)` / `<https://…>` / `![[报告.pdf]]`），粘回笔记即可用；取不到写法时回落通道原文
* **修复（拖入文件夹两处边界）**：① 拖入**库根**时前缀拼成 `'//'`，一个文件都匹配不到 → 整库拖入被当成空文件夹提示「没有文件」（选文件口径下沉为 `links-resolve.filesUnderFolder`，库根特判）；② 批量插入的循环只判 `view.mindMap === null`，而「保存/探测尺寸期间换文件」会**重建引擎**（新实例 ≠ null）→ 旧节点已不在新树上、插入静默丢失；现按「会话所属引擎」守卫（与系统文件拖入同款）
* **修复（手势重复实现）**：画布视口此前在容器上又挂了一套 `wheel`/`pointer` 手势，与引擎自带实现打架——引擎的 `wheel` 监听在 `mindMap.el` 上且**先 `stopPropagation()`**，容器级监听收不到事件（死代码，文档却宣称由它实现「Ctrl+滚轮缩放 / 滚轮平移」）；中键拖则与引擎 `isMiddleMousedown → drag` 的**绝对定位**平移互相覆盖，且 `pointerleave` 让手势半途断掉。现**由引擎独家负责**滚轮与中键拖（官方 Canvas 语义：滚轮平移 / `Ctrl`·`Cmd`+滚轮以指针为锚缩放，`createMindMap` 显式钉住 `mousewheelAction:'move'` 与 `disableMouseWheelZoom:false`），插件只补引擎没有的两件事：抑制浏览器**原生中键自动滚动**（容器 `mousedown` button===1 → preventDefault）、`Shift+1`/`Shift+2` 缩放（且输入框内让位，不再出现「在搜索框打 `!` 触发缩放」）。回归：`tests/view-viewport.test.ts`（只注册一条 mousedown + 选区几何不变量）、`tests/vendor-contract.test.ts`（引擎必须保住 stopPropagation / Ctrl 缩放 / middle-drag 三处形态）、`tests/view-hotkeys.test.ts`（Shift+1/2 让位）
* **性能与简化（2026-09-17 量化优化轮，承接上一条）**：
  * **打开文档首帧不再被图片尺寸探测阻塞**：加载路径此前在首帧渲染**前** `await` 全树图片尺寸探测（冷缓存时串行等 ceil(图数/6) 批解码、单张超时 2500ms）。现改为「起步探测（并发 6、不与首帧串行）→ 首帧先按默认尺寸出画 → 探测完成后按 **data 对象身份 + image 地址**回灌引擎并重渲染一次」——首帧时延不再随图片数增长；代价是尺寸可能在图片就位后跳一下（原先靠阻塞首帧规避）。回灌经 `applyImageSizeCorrectionsToEngine`（对象身份匹配 ⇒ 引擎重建后陈旧条目自然落空，不会盖错节点；值相同不写、有改动才 render 一次）
  * **修复（引擎硬要求 `imageSize`）**：引擎 `createImgNode → getImgShowSize` 直接解构 `data.imageSize`，**缺字段即 TypeError、整图渲染中断**（无头实测对照组：一图缺失 ⇒ `renderer.root` 为空），而解析器不产 `imageSize`。此前只有「加载期探测先于引擎」一条隐式兜底——改为首帧后回灌后，任何「先渲染后校正」路径都必须显式填默认值：新增 `media/images-path.ensureDefaultImageSizes`（同步 O(n) 填默认盒），接入**加载路径**与「把文本节点编辑成图片」（`applyRawNodeContent`，顺带修掉一处存量渲染中断隐患）
  * **状态栏节点计数：实测确认性能模式下不漏计**（无需改动）：`removeNodeWhenOutCanvas` 只把视口外节点摘出 DOM（`removeSelf()` = `group.remove()`），节点实例仍留在 `parent.children` ⇒ 渲染树结构完整；此前注释误写「移出渲染树会漏计」（曾误导多轮分析），已修正。`verify:visual` 新增 count 探针（阈值 1 强制开启性能模式的 151 节点地图：渲染树 151 / DOM 仅 11 组）把该事实钉成契约
  * **自绘接管面经量化后决定不回收**：同形状 31 节点两图实测 —— DOM 元素 8.2（引擎文本）vs 7.5（自绘）/节点（自绘更轻）；空 render 构建器调用 0 次（拖动/缩放不重建内容）；改一个节点文本恰好 1 次构建器调用。真实成本是「每次内容重建 ≈ 每节点 1 次构建器 + 引擎 1 次离屏测宽」，按编辑数线性而非按帧 ⇒ 收窄接管面换不来帧级收益，却要吃图标/编辑入口/锚点契约的用户可见回退，**不回收**。perf 探针新增两条硬断言（idle 构建器 0 / 改文本恰好 1）守住该成本模型
  * **测试**：新增 `tests/engine-image-size.test.ts`（回灌的身份/换图/同值/陈旧守卫 5 例）与 `tests/images-path.test.ts` 两步式探测/默认填充用例；`verify:visual` 扩展为 **10 场景 + 12 探针**（新增 image 回灌探针与 count 计数探针）
* **性能与简化（2026-09-16 优化轮）**：① **设置变更刷新防抖**——「应用到视图」此前每档滑块都跑一轮，而一轮 = 每个打开的视图**销毁并重建引擎 + 全量重渲染 + 工具栏重建**（性能阈值是 step 100 的滑块，拖一次即几十轮），现 `applySettingsToViews` 防抖 250ms（`SETTINGS_APPLY_DEBOUNCE_MS`：短于落盘防抖以保「松手即生效」，并在插件卸载时丢弃挂起项）；② **段序列缓存改 LRU**——原「满了整表清空」会被编辑弹窗的实时预览（每个键入都是新键）冲刷，把正在渲染的热点节点一起刷掉、命中率归零，现命中即提升到队尾、超限只淘汰最旧一条；③ **插入链接少一次整树重排**——`setNodeText` 内部已 `render()`，4 处调用点在其后又渲染一遍（多一次整树重排 + 自绘内容重建），现只在「无文本可写」的分支自行重绘；④ **打开文档每行只解析一次**（plain 块首行此前解析两遍：一遍取显示文本、一遍取图片字段）；⑤ **图片调宽帧内不再读画布矩形**（会话建立时快照一次，帧内只读图片元素矩形——少一次强制布局）；⑥ **引用预检一趟扫描**（合并 `hasNodeReference` + `nodeReferenceHaystack`）；⑦ **装配等待的 10s 超时定时器**在正常路径撤销（此前每次打开文件都留一个空转定时器）。另把「`Node.getData()` = 活引用 / `MindMap.getData()` = 深拷贝」的相反语义收口到 `core/node-data.ts` 并加契约断言（写错方向不会报错、只会静默失效）。量化口径也固化下来：`verify:visual` 新增 **perf 探针**（同形状两棵 30 子节点地图的 DOM 规模预算——自绘侧 foreignObject 必须 30、引擎文本侧 0，每节点元素 ≤12/≤20），实测自绘 **7.3** 元素/节点 vs 引擎文本 **8.2**（接管后引擎跳过全部默认内容，故自绘不比引擎文本更重；其真实成本在离屏测宽与内容重建，与元素数无关），并确认无数据变更的 `render()` 零测量（拖动/缩放不重建内容）
* **清理**：删除随白名单失效的 `constants.isLinkAttachmentExtension` / `isImportableExtension` / `isSystemMediaExtension` 与 `common.onlySupportedFiles` / `common.cannotPreview`（附件口径收敛到 `domain/wikilink.wikilinkTargetIsAttachment`）
* **测试（手势、撤销与性能三轮）**：全量 **46 文件 / 1503 例**（性能轮新增/改写：`tests/node-inline-content.test.ts` 段序列缓存 LRU 2 例、`tests/settings-persist.test.ts` 设置应用防抖 2 例、`tests/feature-teardown.test.ts` 帧内不读画布矩形 1 例、`tests/vendor-contract.test.ts` 两种 `getData()` 语义 2 例；`tests/view-node-actions.test.ts` 3 处断言改为「本模块不再自行整树重排」）。**手势与撤销轮**：新增 `tests/view-viewport.test.ts` 9 例：单条 mousedown 注册、中键抑制、选区几何不变量与顺序、降级回落；`tests/view-node-width.test.ts` 3 例：拖宽结束事件订阅；`tests/node-inline-content.test.ts` +6 例：拖宽宽度落元素；`tests/view-context-menu.test.ts` +4 例：复制链接的文件写法；`tests/links-resolve.test.ts` +4 例：文件夹选文件口径含库根特判）；`npm run verify:visual` 增 **undo 探针**（插入 → BACK 的 DOM 精确还原 + 节点实例同一性——「Ctrl+Z 失效」类缺陷与 Alt 拖拽复制的共同守卫）
* **测试（视口与设置分流轮）**：全量 **47 文件 / 1532 例**（本轮新增：`tests/viewport.test.ts` 的实时容器优先 / `resize` 早于写视口 / 落界可见性三态、`tests/engine-controller.test.ts` 的保存视口被判定不可见则回退居中与补居中个案、`tests/node-inline-content.test.ts` 的段缓存规模封顶）；`npm run verify:visual` 现有 **15 个断言探针**（本轮新增 edit 编辑成本 / perf-switch 性能开关 / render-eco 渲染经济 / history 撤销，另有 opt-in 的 `--perf` 负载块）
* **真实墙钟通道（opt-in）**：`npm run verify:visual -- --perf [--perf-edits N]`——同一页跑「空跑 / 负载」两种入口，按**进程墙钟差**给真实成本（`--virtual-time-budget` 下页内时钟被虚拟化不可用；而虚拟时间只在**空闲**时瞬时推进，真实 CPU 时间会留在墙钟差里且不含启动开销）；打包与写盘移出计时窗口，A/B/B/A 交替各取最小值（单次 A→B 实测出过 −233ms 负差值）。实测 **500 节点 / 非性能模式整树渲染 ≲ 0.6ms**（100 次编辑 ≈ 58ms、300 次 ≈ 89ms）（该口径**不含**命令历史快照——编辑间隔未越历史防抖时引擎会把连打并成一条，含历史后单次编辑 ≈ 43ms，见下条）⇒ 引擎属性重写再优化（局部重绘等）ROI 很低，插件侧「不产生多余渲染」才是正确收尾。默认编辑数自本版起为 **300**（且每次含一条历史快照）：实测 **≈ 13.1s / 300 次 = 43.6ms/次**，与内存轮的单次成本互证
* **长会话内存基线（`--perf` 的内存轮，opt-in）**：强制 GC 后采样堆 / DOM / 命令历史 / 段缓存（内存轮给 Chrome 加 `--js-flags=--expose-gc` 与精确内存读数；编辑间隔取 150ms > 引擎历史防抖 100ms——否则虚拟时钟下 100 次连打被并成 **1 条**历史，读数失去意义）。实测（500 节点）：**唯一的大头是引擎命令历史**——每条整树快照 ≈ **91KB** 字符串（堆内 ≈ 183KB/条），到引擎默认上限 500 条时单视图水位 ≈ **91MB**（520 次编辑实测堆 106.3MB）；**单次编辑真实成本 ≈ 43ms，其中渲染只占 ~0.5ms**（大头是引擎 `addHistory` 的整树深拷贝 + JSON 比对）；**无结构泄漏**（节点组增量 0、离屏测量元素恒 1、段缓存有界），DOM 的少量增量可完整归因为「负载用引擎级 `setNodeText`、富节点因此切回引擎渲染」，生产路径不发生
* **测试（历史内存预算轮）**：全量 **48 文件 / 1537 例**（新增 `tests/engine-history-limit.test.ts` 5 例：预算兑现 / 单调不增 / 实测对照值 163 与 40 / 边界与地板）；`--perf` 默认编辑数 100 → **300**（使 500 节点图的历史满仓并裁剪），内存轮新增三条**确定性硬断言**：上限 = 预算反推值、满仓时条数恰好等于上限、快照堆（字符数 × 2）≤ 30MB 预算

* **合规审计（2026-09-17，对照官方指南与 API 逐项核查）**：以 Obsidian Developer Policies / 提交要求 / Manifest 规范为基准，对照官方 `eslint-plugin-obsidianmd`（与项目同版 0.4.2）、`obsidian` 类型定义（1.13.2）、官方插件模板与 `obsidianmd/obsidian-workflows@v1` 逐项核查，修复 **11 项**（P0 × 1 / P1 × 1 / P2 × 9）。完整判定、逐条证据与证据边界见 [`docs/compliance-audit-2026-09-17.md`](compliance-audit-2026-09-17.md)（报告 §8 为修复记录）：
  * **P0（发布阻断）**：`npm run lint` 此前是**红灯**——`src/features/node-inline-content.ts` 的 `no-unnecessary-type-assertion`（一处类型断言的理由已随 TS lib 失效）。因该插件规则集**正是**社区目录 scanner 所用的规则集，而本仓库以 `--max-warnings 0` 运行，等于 CI 一直处于失败态
  * **P1（许可义务）**：发布资产此前只有 `main.js` / `manifest.json` / `styles.css`，而 `main.js` 约 **72%** 是 vendored 的 MIT / BSD 代码、经 minify 后不含任何版权与许可注释（实测 `@license` / `@preserve` / `Copyright` / MIT 与 BSD 许可正文**均 0 命中**）——MIT 与 BSD 都要求声明随所有副本或实质性部分一起分发。现每个 release 一并附上 **`LICENSE`** 与 **`vendor/THIRD-PARTY-NOTICES.md`**
  * **生命周期泄漏**：`view.ts` 的 `css-change` 订阅、窗口级 `paste` 监听与 9 个视图热键此前只在 Component **卸载**时清理，而视图 `onClose` 并不卸载 ⇒ 同一实例多次开合会逐次叠加（现于 `onClose` 对称回收：`offref` / 按引用 `removeEventListener` / 置空 `scope`）；引擎侧 3 处 `setTimeout`（延时 fit、渲染根轮询链、文本编辑宏任务）在引擎销毁后仍会在途执行（现经 `cancelEngineTimers` 随 `destroyInstance` 统一取消）
  * **ESLint 配置层三处静默放宽**：① 模块边界块以 `{ patterns }` 形式**替换**（而非合并）官方的 `no-restricted-imports`，导致官方 7 条依赖禁令（`axios` / `got` / `ky` / `ofetch` / `superagent` / `node-fetch` / `moment`）在 `src/**` 全部失效——现以常量前缀接入，并用**负向探针实测**确认其真会报错；② domain「零依赖」不拦裸包名（本可兜底的官方 `no-nodejs-modules` 因 `isDesktopOnly: true` 被判为 off）——现补裸说明符闭包（用 `regex` 而非 `group: ['*']`：后者按 `allowRelativePaths: true` 匹配，会误伤 `./md-meta` 这类组内相对引用）；③ `no-unsupported-api` 的 `minAppVersion` 默认由插件从 **cwd** 读 `manifest.json`，换 cwd 即整条静默关闭（同时把 `require` 报成假 `no-undef`）——现从配置侧显式传参，值读自 `manifest.json`（不重复字面量）
  * **vendor 产物来源未钉住**：原有契约都只钉 **API 面**（导出类 / 原型方法 / 命令名 / 事件名令牌），对字节改动无感；现把 sha256 登记进 `vendor/BUILD.md` 并在 `tests/vendor-contract.test.ts` 加**字节级身份断言**
* **CI 与发布门禁加固**：
  * **新增 CSS 检查**（`stylelint.config.mjs` + `npm run lint:css`）：`styles.css` 是发布资产，而官方目录 scanner 在 release 模式**强制**对它跑 stylelint ⇒ 此前「scanner 会查、本地完全不查」的唯一资产就是它。规则面照抄官方 scanner（唯一 error 级规则是 `function-url-scheme-disallowed-list`，即禁止 `url()` 里出现 `http`/`https`/`file`，只允许 `data:`）；浏览器基准 `electron >= 39` 由 `minAppVersion: 1.13.0` 经官方 `ELECTRON_VERSIONS` 映射表推得（**改 `minAppVersion` 须同步该值**）。**当前 `styles.css` 在该规则集下干净**
  * **新增发布元数据护栏**（`scripts/check-release-metadata.mjs` + `npm run check:release`）：本仓库 eslint 把 `versions.json` 排除在 lint 之外，而官方对它有 **error 级**形状校验 ⇒ 此前没有替代检查。现校验 `versions.json` 形状与 semver、`manifest.version` 在表内、**当前版本**的值等于 `minAppVersion`、README 非空
  * **发布流程自带门禁**：tag push **不会**触发 `lint.yml`（其 `on.push` 只匹配 branches，不匹配 tag），故 `release.yml` 在构建前先跑 `check:release` + `npm test` + `npm run lint` + `npm run lint:css`——任一失败即不产出 release
* **测试（合规审计轮）**：全量 **48 文件 / 1540 例**（新增 `tests/vendor-contract.test.ts` 的 vendor 产物 sha256 字节级身份断言）

<h3>兼容性说明</h3>

* 与 0.1.1 一致：**需要 Obsidian 1.13.0+**，**仅桌面端**（`isDesktopOnly: true`）
* **每个 release 现另附 `LICENSE` 与 `vendor/THIRD-PARTY-NOTICES.md`**（第三方许可声明随产物一起分发，满足 MIT / BSD 的「声明随所有副本或实质性部分」要求）；安装插件本身仍只需 `main.js` / `manifest.json` / `styles.css`
* 新增两条命令（`mindmap-split-links` / `mindmap-split-links-all`）与一个设置项「自动拆分混排双链」（默认开启）；既有命令 ID 与 `.mindmap.md` 格式均未变更
* 链接写入语义修订见上——依赖 0.1.1「无条件覆盖」或旧「纯双链节点改链」行为的用户请留意；回写保真的「额外 token 附于行尾」说明见修复条目
* **设置面板调整不再重建视图**：主题 / 性能模式与阈值改为原地生效（视图不销毁重建、视口与选中状态不受影响）；拖拽开关与语言仍会重建（引擎侧只有创建期通道）

---

<h3 id="en-v0.1.2">Added</h3>

* **Mixed wikilink splitting**: document / attachment wikilinks mixed with descriptive text (e.g. `about [[winter]] and [[autumn]]`) can be extracted into child nodes — the parent keeps its text (each link replaced by its display name) and every link becomes its own child node (with icon / hover / click). Three entry points: ① command "Split links into child nodes"; ② **automatic splitting** after editing such a node (setting "Auto-split mixed links", **on by default**; the check runs against nodes captured while editing, so switching selection right after committing an edit never misses a split); ③ command "Split all mixed links in document" for the whole note (including untouched nodes). Images and external URLs are never split, link reference definition lines are skipped, identical targets are deduplicated, and repeated runs are no-ops
* **Paragraph-node first-line images**: an inline `![[img.png]]` on the first line of a paragraph (text) node now renders as the node image (replaceable / removable); link syntax inside paragraphs is still preserved verbatim
* **Rich node content (option B)**: links inside a node render as **clickable text** (click to open, `Ctrl`/`Command`+click for a new tab) — **every link on a line is clickable** (previously only the first one) and hovering previews **the anchor under the pointer**; **a URL mixed with other content becomes a clickable address** (previously always icon-only); light markup `**bold**` / `*italic*` / `` `inline code` `` / `~~strikethrough~~` renders inside nodes (**including nodes without any link**) while the raw text stays verbatim in the file; **links inside paragraph (multi-line) nodes are clickable too**. Nodes with links / markup / very long text are edited through a **plugin text dialog** (double-click / right-click "Edit text" / F2 — the dialog holds the **full text**), and self-drawn content carries its styles inline so PNG export keeps them
* **Truncated display for very long nodes**: nodes over 2000 characters render only their beginning plus `…` (with a hover explanation); the full text stays in the file and is available by double-clicking to view / edit
* **Node edit dialog: source mode** (the edit entry now branches by node type): a **pure link node** still edits its **alias** (semantics unchanged); every other rich node (mixed links / URLs / markup / very long text) now edits **the actual line from the file** — `[[wikilink]]`, URL and `**bold**` syntax stay visible and editable, with a live "**the node will show**" preview, and saving writes that line back **verbatim** (re-parse → the inline fields are rebuilt wholesale ⇒ back to the "unedited" state). Previously these nodes edited the **display text**: wikilinks were reduced to their bare label and icon-only URLs were invisible (user feedback)
* **In-place multi-token write-back**: multiple links / URLs / images on one line are written back at their **original positions** after editing (previously they were always appended at the end); only when a position cannot be located do they fall back to appending
* **Interaction aligned with Obsidian's official behaviour** (checked line by line against the official help vault): ① **two more link-open destinations** — `Ctrl`/`Command`+`Alt`+click opens a **new tab group**, adding `Shift` opens a **new window** (previously only "new tab" existed); ② **unresolved links are muted** — a link whose target does not exist renders in a **muted colour** (like the reading view) and no longer previews on hover; ③ **inline syntax now covers the official list** — `==highlight==`, `__bold__` / `_italic_`, `***bold italic***` and double-backtick code spans, plus correct handling of backslash escapes (`\*a\*` shows `*a*`, which used to be rendered as italic) and `%%comments%%` (hidden inside the node, kept in the file); ④ **dropping files from your system accepts any type** (a type allow-list was removed), embeddable attachments are written as embeds `![[report.pdf]]`, and holding **`Ctrl` (Win/Linux) / `Option` (mac)** **skips the copy** and inserts an absolute link `[file name](<file:///…>)` that opens in your system's default app — the same behaviour as Obsidian's drag & drop

<h3>Fixed</h3>

* **Write-back fidelity: two inline images on one line**: `![[a.png]] and ![[b.png]]` used to have its second embed downgraded to plain filename text on **any save** (misdetected as a "leftover link"); image embeds are now recognised by target type and preserved
* **Write-back fidelity: multiple links / URLs / images on one line**: editing such a node used to downgrade the non-first tokens to text (a second bare URL even lost its content entirely); all tokens are now preserved verbatim — nothing is lost, but to keep the line structure stable the extra tokens are appended at the end of the line, so their **position may differ from the original** (superseded later in this version by **in-place write-back**, see the last Added bullet)
* **Write-back fidelity: link reference definition lines** (`[ref]: …` / `[^1]: …`): editing a definition line containing a bare URL used to move the URL to the end and break every `[ref]` reference; definition lines are now kept as plain text, and the splitting command skips them as well
* **frontmatter is saved from the current on-disk value** (data safety): before writing, the file is re-read and the header is taken from the **current disk content** — properties changed in the properties panel / other panes / other devices (sync) while the view is open are no longer overwritten by the load-time snapshot (previously an occasional "lost note properties")
* **frontmatter parsing boundaries aligned with Obsidian**: an empty property block (`---` directly followed by `---`) is no longer merged into the body; a `---` inside a YAML block scalar (`|` / `>`) no longer truncates the properties; a leading BOM is kept in the header (previously it could demote the first heading to a paragraph and add a phantom indent to the first list item)
* **Images: load-time auto sizing is no longer written back**: display sizes computed from the natural aspect ratio (not a user intent) no longer produce `|width` — previously an untouched line could gain a size parameter after saving
* **Images: "Remove image" no longer brings the image back on save**: removing an image from a list / heading node used to write it back on the next save (once the image write-back metadata was cleared, the "image removed" detection no longer triggered and the line was written verbatim); detection no longer relies on leftover fields — an empty `image` with inline image syntax still present (`![[…]]` / `![…](…)`) is treated as removed, matching paragraph nodes
* **Splitting: runtime resource addresses no longer leak into notes**: image-node splitting used to treat runtime resource addresses (`app://…#img.png?timestamp`) as attachments and write URL-encoded fragments into the parent text; splitting now shares the serializer's `app` context and bails out entirely when `app://` appears
* **Deleting a note silently downgraded links to plain text** (data safety, user feedback): deleting a note (or moving it to `.trash`) used to clear the link references on mind-map nodes — while leaving the node text and the inline ledger untouched — so the next save wrote `[[NoteA]]` as plain text `NoteA` (other links on the same line kept their syntax because their ledger entries survived, which shows up as "the first link got downgraded"), **irreversibly**. Now: **links are rewritten, never removed** — deleting / trashing keeps them as **unresolved links** (matching Obsidian; recreating a note of the same name restores them) and renaming still rewrites them; **attachments are removed wholesale** — the reference *and* its visible text disappear (clearing only the reference left a plain-text `report.pdf` residue), so an emptied **leaf node is pruned together with the reference**, while a node with children keeps the reference instead of losing its subtree; **inline images are still cleared on delete** (a missing target would leave a broken image block in the node)
* **Very long nodes caused a white screen / freeze-then-crash**: the root cause sits in the engine's default SVG text path — it wraps **character by character**, rebuilding the whole candidate line and **re-measuring it** for every character, so a single line's cost grows **quadratically** (measured on the same machine in headless Chrome: a 20k-character line ≈ **+2.8 s per node**, whole run 1.1 s → 3.9 s) — and the previous "fall back to the engine for over-long text" strategy fed the longest lines into exactly that path. Over-long text is now taken over by the self-drawn renderer and **truncated**: the same load is back to **1.1 s** (identical to a run with no long text at all)
* **Light markup did nothing on nodes without links**: `**bold**` only rendered on nodes that also carried a link; now "has a link **or** light markup" takes over
* **PNG export lost styles**: layout / typography / colours / markup styles of self-drawn content are now **all inlined on the elements** (the engine's export injects only its own CSS and never loads the plugin stylesheet; relying on class rules used to break the exported layout)
* **Double-click did nothing on rich nodes**: the engine's inline editor silently no-ops for self-drawn nodes; double-click / right-click "Edit text" / F2 now all route to the plugin dialog (which holds the full text)
* **The first render of an opened map was offset** (user feedback): opening a `.mindmap.md` / document-mode map placed the content off to one side, and "Fit canvas" brought it right back (which made it look like an occasional glitch). The root cause was **stale geometry in the first-frame centring** — the engine caches the container rect (`elRect` / `width` / `height`) only at creation and on `resize()`, while the workspace layout settling and the document-mode toolbar rebuild after the first paint change the container's size and position **without firing `view.onResize`**, so both the zoom anchor and the content-box conversion were computed against the old canvas. The viewport code now syncs the engine geometry to the **live container** before computing anything (`getLiveCanvasRect` / `syncCanvasGeometry`), and the content box is measured against the live container's origin as well
* **A saved viewport could push the content out of the canvas**: a saved viewport is recorded against the container and content layout of its time, so restoring it after a large change could leave the view outside the canvas (blank on open). Restoring is now followed by a **visibility check**: a viewport that is definitely off-canvas is dropped in favour of the default centring, while an undecidable case (performance mode, engine mid-state) is kept, so a legitimate user viewport is never harmed
* **"Root node centred" flashed before "whole map centred"** (visible jump): the engine's default `initRootNodePosition = [center, center]` puts the **root node** at the canvas centre on the first layout, while the plugin applied its viewport from a 150 ms fallback timer — so the intermediate state was painted and then jumped. The viewport is now applied **inside the first layout's completion callback** (`node_tree_render_end`, part of the layout task, so the browser never paints the intermediate state), with a budget of 2 recalculations for the window in which content can still change (relayouts triggered by `resize`, image-size corrections); after that the plugin stops touching the viewport
* **Centring for maps with images is correct once image sizes settle**: with image-size probing no longer blocking the first frame, the real sizes arriving later change node sizes and the content box; the plugin now recentres after that correction (**only while the current viewport is still the one it set automatically on open**) — if you have already panned or zoomed, it never interferes

<h3>Performance</h3>

* **Image resize write coalescing**: drag updates are merged by a minimum step (8px) with a final write on release — re-layout cost drops by an order of magnitude; stale frames are validated against the session's engine and never written into a new one
* **True LRU image-size cache**: hits are promoted, so hot images are no longer evicted by cold ones and re-decoded
* **Cross-session size cache**: via the official `App#saveLocalStorage`, isolated per vault — reopening the same vault re-decodes nothing (external URLs and failures are not persisted; corrupted data degrades silently)
* **Image dialog**: previews are deduplicated by parse result; intermediate input no longer rebuilds the DOM on every keystroke
* **Per-node render cost is capped**: the self-drawn path is bounded at 2000 characters per node regardless of the text length (over-long nodes no longer get slower as text grows), and inline segments are cached by **source content**, so a line is not re-parsed during drag / zoom
* **Settings changes now take effect in place, without rebuilding the engine**: applying settings used to rebuild every open view (deep-cloning the tree, destroying and recreating the engine, a full re-render and a toolbar rebuild), and the performance threshold is a step-100 slider — one drag meant dozens of rounds. Changes are now routed by a **key diff**: **theme** (`setThemeConfig`) and **performance mode / threshold** (`updateConfig`, which the engine explicitly supports at runtime) apply **in place**; **default layout / default line style** cannot affect an already-open map and are skipped; only the **drag toggle** and **language** still rebuild (the engine exposes creation-time channels only: dragging is handled by the Drag plugin registered at creation, and the language is captured in closures at creation)
* **Render economy: at most one layout pass per action**: auditing every `render()` call on the plugin side found and removed one duplicate (disabling performance mode rendered twice for an identical DOM); `verify:visual` gained a render-eco probe that pins the layout-pass counts of 8 representative actions as a regression gate
* **A memory budget for undo depth on large maps**: the command history stores full-tree JSON snapshots at ≈ 0.4 KB per node, and a fixed entry cap let a single view's history grow with the map (the old policy held ≈ 91 MB for a 500-node map — more than the ≈ 70 MB of a 2000-node one). The entry cap is now derived from a **30 MB-per-view budget**: 500 nodes → **163 entries**, 2000 nodes → **40**, with a floor of 30 for very large maps (small maps whose budget is ample keep the engine default of 500) — a single view's history stays within the budget while undo remains far deeper than everyday use needs

<h3>Behaviour changes</h3>

* **Link write semantics revised again** (0.1.1 "unconditional overwrite" → now **overwrite only for a completely blank node**): attaching a link (document / attachment / URL) to a node that **already has any content** (text / an existing link / an image / inline tokens) now always creates a **child node** and leaves the node untouched — inline multi-link lines are no longer disturbed by overwrites; only a **completely blank** node is replaced by the link node. A URL **on its own node** stays icon-only (that node has no text; mixed with other content it shows as a clickable address). Note: the one-step "re-link" entry is gone — to change a link, clear it first and add the new one, or edit the node text directly
* **Links inside nodes are now clickable text** (previously only the first one was clickable and URLs were icon-only); nodes with links / markup / very long text **no longer have the engine's inline editor** — double-clicking opens the plugin dialog
* **The edit dialog has two modes** (see the Added section): a pure link node edits its **alias** (unchanged); every other rich node edits **the source line** (previously the display text was edited, hiding link syntax and URLs). In source mode, cancelling or leaving the value unchanged writes nothing
* **Nodes over 2000 characters show only their beginning** (marked with `…`, with a hover explanation); the file content and `data.text` are untouched, and double-clicking shows / edits the full text
* **Image nodes are unaffected by these rendering changes**: images still go through the engine's image pipeline (mixed text on image nodes no longer stacks the document-page icon); nodes that combine an **image with very long text** still rely on the engine's wrapper (they cannot be taken over) — consider splitting such a node
* **`F2` now matches Obsidian**: with **no node selected** F2 falls through to Obsidian and **renames the current file** (the official meaning); with a node selected it still edits that node (selecting the central node amounts to the same thing, since its text *is* the file name). Previously the key was swallowed unconditionally, so Obsidian's own F2 was unusable inside the mind-map view
* **New setting "Automatically update links"** (mirrors the official "Automatically update internal links", **on by default**): turning it off stops link rewriting on rename (links become unresolved) and skips reference/attachment cleanup on delete — the same as Obsidian with that setting disabled
* **Link syntax now follows the official Wikilink setting**: with `Use \[\[Wikilinks\]\]` **on** (the official default) nothing changes (`[[wikilinks]]` and the document-page icon are kept); when a user turns it **off**, new document links are written as `[label](path.md)` (via the `hyperlink` channel), and renaming rewrites that form too (previously only `[[…]]` was recognised, so md-form links broke after a rename)
* **The dragged-file type allow-list is gone**: vault files, system files and the link dialog now share the **parser's definition** of an attachment (anything with a non-document extension; no extension means a document). A hand-written `[[notes.txt]]` already displayed, and dropping `.txt` now attaches as well (previously the drop was rejected while the hand-written form worked — the two sides disagreed). Three obsolete allow-lists and two obsolete strings were deleted along with it
* **Clicking a non-renderable vault file now opens it in your system's default app** (previously only audio/video did; everything else showed "cannot preview") — with the allow-list gone, any extension can end up in the vault, and a notice would mean "linked but unopenable"
* **Fewer undo steps on large maps**: the history cap is now derived from a memory budget (see Performance) — the undo depth shrinks as the map grows (163 steps at 500 nodes, 40 at 2000, floor of 30), keeping a single view's history under 30 MB

<h3>Engineering</h3>

* **Tests** (full suite: 42 files / 1327 cases): added `tests/view-split-links.test.ts` (auto-split: setting toggle / untouched / editing state and candidate-set semantics) and `tests/agents-md-sync.test.ts` (**docs-code sync contract**: every file under `src/**` and `tests/**` must be registered in AGENTS.md — an omission turns a test red); `tests/links-split.test.ts` expanded into a full rule matrix (including definition-line skip and post-split multi-token rebuild); `tests/md-roundtrip.test.ts` gained the write-back fidelity group (two inline images, multi-link / multi-URL / multi-image fidelity after editing, definition lines in 4 forms plus non-regression controls); further coverage for frontmatter (CRLF / empty block / block scalar / BOM), image sizing and caching, auto-split candidates and engine-event forwarding contracts
* **Docs**: AGENTS.md restructured into a **K-numbered, grouped system** (stable reference IDs K1–K49 + "new-feature checklist" + "maintenance rules for this file"); mapping standard refreshed (splitting rules, write-back fidelity and the known-degradation table); README link-write semantics corrected
* **Checks**: type-check 0 errors, eslint 0 warnings, full test suite green
* **Tests (this round)**: the full suite is now **44 files / 1401 cases**; added `tests/node-inline-content.test.ts` (segments, takeover rules, light markup, truncation, inline-style contract), `tests/md-line-write.test.ts` (source-mode write-back: wholesale field rebuild, verbatim round-trip, multi-line, unclosed syntax), 6 anchor-level hover cases in `tests/view-wikilink.test.ts`, the edit-entry mode split in `tests/view-hotkeys.test.ts`, and ledger registration boundaries in `tests/md-inline.test.ts`; `npm run verify:visual` grew to **9 scenarios + 6 probes** (self-drawn assembly, measurement parity, a synthesised click hitting the anchor, inline export styles, 30-rich-node scale, truncation, clickable paragraphs)
* **Single ledger**: the extra-token field `mdExtraTokens` is merged into `mdSegments` (`first` distinguishes first vs extra tokens) — parsing, split rebuild and link clearing now each have exactly one implementation
* **Tests (alignment round)**: the full suite is now **44 files / 1437 cases**; `tests/node-inline-content.test.ts` gained light-markup extensions, the escape set, comments and the unresolved-link marking; `tests/view-wikilink.test.ts` gained the modifier matrix and "no preview for unresolved targets"; `tests/view-dnd.test.ts` was rewritten around "any type can be imported / `Ctrl` absolute links / extensionless files take the document channel"; `tests/url.test.ts` gained `file:///` round-trips in all three path forms; `tests/view-node-actions.test.ts` gained `applyExternalLink` and `mdEmbed`; `npm run verify:visual` grew to **10 scenarios + 6 probes** (new `syntax` scenario: `<mark>` / `__bold__` / escapes / comments / `is-unresolved`, with two negative self-checks — a stub that always resolves, and comments left visible — both verified to turn the run red)
* **Cleanup**: removed the allow-lists and strings that became obsolete — `constants.isLinkAttachmentExtension` / `isImportableExtension` / `isSystemMediaExtension` and `common.onlySupportedFiles` / `common.cannotPreview` (the attachment definition now lives in `domain/wikilink.wikilinkTargetIsAttachment`)
* **Tests (viewport & settings-routing rounds)**: the full suite is now **47 files / 1532 cases** (new this round: live-container priority, `resize` before the viewport write, and the three-state visibility check in `tests/viewport.test.ts`; saved-viewport fallback and recentring in `tests/engine-controller.test.ts`; the segment-cache size cap in `tests/node-inline-content.test.ts`); `npm run verify:visual` now has **15 asserting probes** (new this round: edit / perf-switch / render-eco / history) plus the opt-in `--perf` workload block
* **Real-clock channel (opt-in)**: `npm run verify:visual -- --perf [--perf-edits N]` runs the same page as "idle" vs "workload" and reports the **process wall-clock difference** as the real cost (under `--virtual-time-budget` in-page clocks are virtualised and useless, but virtual time only fast-forwards through idle periods, so real CPU time stays in the wall-clock difference, free of startup cost); bundling and file writes are excluded from the timing window, and an A/B/B/A alternation takes the minimum per variant (a single A→B pair produced a −233 ms negative delta in testing). Measured: **a full tree render of 500 nodes ≲ 0.6 ms** (100 edits ≈ 58 ms / 300 edits ≈ 89 ms) (this figure **excludes** the command-history snapshot — edits closer together than the engine's history debounce collapse into one entry; history included, an edit costs ≈ 43 ms, see the next bullet) ⇒ further optimisation of the engine's attribute writes (partial re-render etc.) has very low ROI, while "never render more than once per action" on the plugin side is the right place to stop. The default edit count is **300** as of this version (each edit also carrying one history snapshot): measured **≈ 13.1 s / 300 edits = 43.6 ms per edit**, cross-checking the memory round's per-edit figure
* **Long-session memory baseline (the `--perf` memory round, opt-in)**: samples the heap / DOM / command history / segment cache **after a forced GC** (the memory round adds `--js-flags=--expose-gc` and precise memory readings to Chrome, and spaces edits 150 ms apart — above the engine's 100 ms history debounce, otherwise 100 rapid edits collapse into a **single** history entry under virtual time and every reading becomes meaningless). Measured (500 nodes): **the engine's command history is the only significant item** — each full-tree snapshot is ≈ **91 KB** of JSON strings (≈ 183 KB in the heap), so at the engine's default cap of 500 entries a single view holds ≈ **91 MB** (106.3 MB measured after 520 edits); **one edit really costs ≈ 43 ms, of which the render is only ~0.5 ms** (the rest is the engine's `addHistory` deep clone + JSON comparison); **there is no structural leak** (node-group delta 0, one offscreen measurement element, a bounded segment cache), and the small DOM growth is fully attributable to the workload writing through the engine-level `setNodeText` (which switches rich nodes back to engine rendering) — production's source write-back never does that
* **Tests (history memory-budget round)**: the full suite is now **48 files / 1537 cases** (new: the 5 cases of `tests/engine-history-limit.test.ts` — budget honoured / monotone / the documented 163 and 40 / boundaries and floor); the default `--perf` edit count went 100 → **300** (so the 500-node map's history fills and trims), and the memory round gained three **deterministic assertions**: the cap equals the budget-derived value, a saturated history sits exactly at the cap, and the snapshot heap (characters × 2) stays within the 30 MB budget

* **Compliance audit (2026-09-17, checked item by item against the official guidelines and API)**: benchmarked against Obsidian's Developer Policies / submission requirements / manifest reference, the official `eslint-plugin-obsidianmd` (the same 0.4.2 the project uses), the `obsidian` type definitions (1.13.2), the official plugin template and `obsidianmd/obsidian-workflows@v1`. **11 items fixed** (P0 × 1 / P1 × 1 / P2 × 9); the full verdict, per-item evidence and evidence boundaries are in [`docs/compliance-audit-2026-09-17.md`](compliance-audit-2026-09-17.md) (section 8 is the fix log):
  * **P0 (release blocker)**: `npm run lint` was **red** — `no-unnecessary-type-assertion` in `src/features/node-inline-content.ts` (the rationale for one type assertion had gone stale with the TS lib). That plugin ruleset is **exactly** the one the community-directory scanner uses, and this repo runs it with `--max-warnings 0`, so CI had been failing all along
  * **P1 (licence obligation)**: release assets used to be only `main.js` / `manifest.json` / `styles.css`, while `main.js` is about **72%** vendored MIT / BSD code and, being minified, carries no copyright or licence notice at all (measured: `@license` / `@preserve` / `Copyright` and the MIT & BSD permission texts are **all zero hits**) — both MIT and BSD require the notice to ship with every copy or substantial portion. Every release now also attaches **`LICENSE`** and **`vendor/THIRD-PARTY-NOTICES.md`**
  * **Lifecycle leaks**: the `css-change` subscription, the window-level `paste` listener and the 9 view hotkeys were only cleaned up when the Component is **unloaded**, which `View.onClose` does not trigger ⇒ repeated close/open cycles on one instance stacked them (they are now released symmetrically in `onClose`); on the engine side, three `setTimeout` calls (deferred fit, the render-root polling chain, the text-edit microtask) could still fire after the engine had been destroyed (now cancelled together with `destroyInstance` through `cancelEngineTimers`)
  * **Three silent widenings in the ESLint config**: ① the module-boundary blocks **replaced** (rather than merged with) the official `no-restricted-imports`, dropping all seven official dependency bans (`axios` / `got` / `ky` / `ofetch` / `superagent` / `node-fetch` / `moment`) across `src/**` — they are now injected as a shared constant prefix, and a **negative probe** confirmed they really fire; ② the domain "zero dependencies" rule did not catch bare specifiers (the official `no-nodejs-modules` that could have covered it is `off` because `isDesktopOnly: true`) — a bare-specifier closure was added, using `regex` rather than `group: ['*']` (the latter matches with `allowRelativePaths: true` and wrongly flags intra-group relative imports such as `./md-meta`); ③ `no-unsupported-api` read `minAppVersion` from `manifest.json` **relative to the cwd**, so any other cwd silently disabled the whole rule (and produced a bogus `no-undef` on `require`) — the version is now passed explicitly from the config, read out of `manifest.json` (no duplicated literal)
  * **Vendor provenance was not pinned**: every existing contract pinned only the **API surface** (exported classes / prototype methods / command names / event-name tokens) and was blind to byte-level changes; the sha256 is now recorded in `vendor/BUILD.md` and asserted **byte-for-byte** in `tests/vendor-contract.test.ts`
* **CI and release gating hardened**:
  * **New CSS check** (`stylelint.config.mjs` + `npm run lint:css`): `styles.css` is a release asset and the official directory scanner **forces** stylelint on it in release mode, yet there was no CSS check locally at all — it was the one asset the scanner checked and the repo did not. The ruleset mirrors the official scanner (its single error-level rule is `function-url-scheme-disallowed-list`, banning `http`/`https`/`file` inside `url()` and allowing only `data:`); the browser baseline `electron >= 39` is derived from `minAppVersion: 1.13.0` via the official `ELECTRON_VERSIONS` table (**keep it in sync when bumping `minAppVersion`**). `styles.css` is currently clean under that ruleset
  * **New release-metadata guard** (`scripts/check-release-metadata.mjs` + `npm run check:release`): this repo's eslint excludes `versions.json`, while the official workflow validates its shape at **error** level ⇒ there was no replacement check. It now validates the shape and semver of `versions.json`, that `manifest.version` is listed, that the **current** version's value equals `minAppVersion`, and that the README is non-empty
  * **The release workflow now gates itself**: pushing a tag does **not** trigger `lint.yml` (its `on.push` matches branches, not tags), so `release.yml` runs `check:release` + `npm test` + `npm run lint` + `npm run lint:css` before building — any failure means no release is produced
* **Tests (compliance-audit round)**: the full suite is now **48 files / 1540 cases** (new: the byte-level vendor sha256 identity assertion in `tests/vendor-contract.test.ts`)

<h3>Compatibility notes</h3>

* Unchanged from 0.1.1: **requires Obsidian 1.13.0+**, **desktop only** (`isDesktopOnly: true`)
* **Every release now also ships `LICENSE` and `vendor/THIRD-PARTY-NOTICES.md`** (third-party licence notices travel with the artefacts, as MIT / BSD require); installing the plugin itself still needs only `main.js` / `manifest.json` / `styles.css`
* Two new commands (`mindmap-split-links` / `mindmap-split-links-all`) and one setting ("Auto-split mixed links", on by default); existing command IDs and the `.mindmap.md` format are unchanged
* The link write-semantics revision is described above — users relying on 0.1.1's unconditional overwrite or the previous "re-link a pure-wikilink node" behaviour should take note; see the Fixed section for the "extra tokens appended at line end" note on write-back fidelity (now superseded by in-place write-back, see Added)
* No new settings or commands; the `.mindmap.md` format and all existing command IDs are unchanged. Rendering / editing behaviour changes are listed above (clickable node links, truncated over-long nodes, dialog-based editing for rich nodes)
* **Changing settings no longer rebuilds the view**: theme / performance mode and threshold now apply in place (no engine rebuild, viewport and selection untouched); the drag toggle and language still rebuild (the engine exposes creation-time channels only)
